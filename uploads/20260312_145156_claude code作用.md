我在Claude code中使用其他模型的时候，
Claude code本身的作用是什么？

整体可以，但是需要实现相应后端：
- 实现邮箱或者GitHub注册登录功能
- 登录注册后用户可以评论，发帖（可选）
- 帖子支持markdown语法


Role: Full Stack Senior Architect
Task: Transform a static HTML/JS blog into a dynamic Node.js application.

Context:
I have a frontend-only blog with the following structure:
- `index.html`: Main UI (Galaxy theme).
- `js/app.js`: Main logic (currently uses static data and localStorage).
- `js/data.js`: Static article data (to be replaced).
- `js/galaxy.js`: Visual effects (keep as is).

Tech Stack Strategy:
- **Backend**: Node.js with Express.
- **Database**: SQLite (via Prisma ORM) for easy setup and portability.
- **Auth**: Passport.js (GitHub Strategy) + JSON Web Tokens (JWT) for local email/password.
- **Markdown**: Use `marked` library on the frontend to render markdown content.

Please execute the following Implementation Plan step-by-step. Do not try to do everything at once. Wait for my confirmation after each phase.

### Phase 1: Project Initialization & Server Setup
1. Initialize `package.json` and install dependencies: `express`, `cors`, `helmet`, `morgan`, `prisma`, `@prisma/client`, `dotenv`.
2. Setup the directory structure:
   - `/server` (controllers, routes, middleware, prisma schema)
   - `/public` (move existing HTML/CSS/JS files here).
3. Create a basic `server/index.js` that serves the static files from `/public` and listens on port 3000.
4. Verify the site loads correctly as a static site served by Express.

### Phase 2: Database Layer (Prisma & SQLite)
1. Initialize Prisma with SQLite.
2. Define the Schema (`schema.prisma`):
   - **User**: id, email, passwordHash (nullable for GitHub users), githubId, avatarUrl, username, createdAt.
   - **Article**: id, title, content (Markdown), excerpt, tags (String), views, createdAt, updatedAt.
   - **Comment**: id, content, userId, articleId, createdAt.
3. Run migrations (`npx prisma migrate dev`).
4. Create a seed script (`prisma/seed.js`) to migrate the data currently in `js/data.js` into the SQLite database.

### Phase 3: Authentication API (The Hard Part)
1. Install `bcrypt`, `jsonwebtoken`, `passport`, `passport-github2`.
2. Implement User Registration/Login API:
   - `POST /api/auth/register` (Email/Password).
   - `POST /api/auth/login` (Returns JWT).
   - `GET /api/auth/github` & callback (Handle GitHub OAuth flow).
   - `GET /api/auth/me` (Verify token and return current user info).
3. Create an Auth Middleware to protect routes.

### Phase 4: Core Logic API
1. **Articles**:
   - `GET /api/articles` (Support query params for search/filter).
   - `GET /api/articles/:id`.
   - `POST /api/articles` (Protected, supports Markdown content).
2. **Comments**:
   - `GET /api/articles/:id/comments` (Include User info).
   - `POST /api/articles/:id/comments` (Protected).

### Phase 5: Frontend Integration (Refactoring `app.js`)
*Critial Note: Do not break the Galaxy Canvas effects.*
1. **Markdown**: Add `<script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>` to `index.html`.
2. **API Client**: In `js/app.js`, create a `ApiClient` class to handle `fetch` requests with JWT headers.
3. **Data Loading**: Replace `this.articles = window.articles` in `BlogApp` with an async `fetch('/api/articles')`.
4. **Rendering**: Update `openArticle` to use `marked.parse(article.content)` instead of raw HTML insertion.
5. **Comments**: Remove LocalStorage logic. Fetch comments from API. Update "Submit Comment" to post to API (check for login first).

### Phase 6: UI Additions for Auth
1. Create a Login/Register Modal in `index.html` (match the existing glassmorphism style).
2. Update the Header:
   - If not logged in: Show "Login".
   - If logged in: Show "Username" and "New Post" button.
3. Wire up the Login forms to the `ApiClient`.

Let's start with **Phase 1**. Please guide me through the setup.



角色：全栈高级架构师
任务：将静态 HTML/JS 博客转型为动态 Node.js 应用程序。

背景说明：
我现有一个纯前端博客，其结构如下：
- `index.html`：主界面（银河主题）。
- `js/app.js`：主要逻辑（目前使用静态数据和本地存储）。
- `js/data.js`：静态文章数据（待替换）。
- `js/galaxy.js`：视觉特效（保留原样）。

技术栈策略：
- **后端**：Node.js 搭配 Express 框架。
- **数据库**：采用 SQLite（通过 Prisma ORM），便于快速部署与跨平台移植。
- **认证**：使用 Passport.js（GitHub 策略）结合 JSON Web Tokens（JWT）实现本地邮箱/密码验证。
- **Markdown**：在前端使用 `marked` 库渲染 Markdown 内容。

请按以下实施计划逐步执行。请勿一次完成所有任务。每个阶段结束后请等待我的确认。

### 阶段 1：项目初始化与服务器设置
1. 初始化 `package.json` 并安装依赖：`express`、`cors`、`helmet`、`morgan`、`prisma`、`@prisma/client`、`dotenv`。
2. 设置目录结构：
   - `/server` (控制器、路由、中间件、Prisma 模式)
   - `/public` (将现有的 HTML/CSS/JS 文件移动至此)
3. 创建一个基础的 `server/index.js`，用于从 `/public` 提供静态文件并在端口 3000 上监听。
4. 验证站点作为 Express 提供的静态网站能正确加载。

### 第二阶段：数据库层（Prisma 与 SQLite）
1. 使用 SQLite 初始化 Prisma。
2. 定义模式（`schema.prima`）：
- **用户**: ID、邮箱、密码哈希（GitHub 用户可为空）、GitHub ID、头像链接、用户名、创建时间。
- **文章**: ID、标题、内容（Markdown 格式）、摘要、标签（字符串类型）、浏览次数、创建时间、更新时间。
- **评论**: ID、内容、用户 ID、文章 ID、创建时间。
3. 运行数据库迁移（`npx prisma migrate dev`）。
4. 创建种子脚本（`prisma/seed.js`），将当前 `js/data.js` 中的数据迁移到 SQLite 数据库。

### 第三阶段：身份验证 API（困难部分）
1. 安装 `bcrypt`、`jsonwebtoken`、`passport`、`passport-github2`。
2. 实现用户注册/登录 API：
- `POST /api/auth/register`（邮箱/密码注册）。
- `POST /api/auth/login`（返回 JWT 令牌）。
- `GET /api/auth/github` 及回调接口（处理 GitHub OAuth 认证流程）。
- `GET /api/auth/me`（验证令牌并返回当前用户信息）。
3. 创建认证中间件以保护路由。

### 第 4 阶段：核心逻辑 API
1. **文章管理**：
   - `GET /api/articles`（支持搜索/筛选的查询参数）。
- `GET /api/articles/:id`（获取指定 ID 的文章）。
- `POST /api/articles`（受保护接口，支持 Markdown 内容）。
2. **评论功能**：
- `GET /api/articles/:id/comments`（包含用户信息）。
- `POST /api/articles/:id/comments`（受保护）。

### 阶段 5：前端集成（重构 `app.js`）
*关键注意事项：请勿中断 Galaxy Canvas 特效。*
1. **Markdown**：在 `index.html` 中添加 `<script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>`。
2. **API 客户端**：在 `js/app.js` 文件中创建 `ApiClient` 类，用于处理附带 JWT 请求头的 `fetch` 请求。
3. **数据加载**：将 `BlogApp` 中的 `this.articles = window.articles` 替换为异步调用 `fetch('/api/articles')`。
4. **渲染处理**：将 `openArticle` 函数中的原始 HTML 插入方式更新为使用 `marked.parse(article.content)`。
5. **评论功能**：移除 LocalStorage 相关逻辑。通过 API 获取评论数据，并将"提交评论"功能改为向 API 发送请求（需先检查登录状态）。

### 第六阶段：身份验证相关界面新增
1. 在 `index.html` 中创建登录/注册模态窗口（需匹配现有玻璃拟态风格）。
2. 更新页眉部分：
   - 若未登录状态：显示“登录”。
- 如果已登录：显示"用户名"和"发新帖"按钮。
3. 将登录表单连接至`ApiClient`。

现在开始**第一阶段**。请引导我完成设置步骤。